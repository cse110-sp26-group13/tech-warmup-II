const GAME_CONFIG = {
  initialBalance: 100,
  fixedBet: 10,
  reelCount: 3,
  symbols: ["🍒", "🍋", "🔔", "💎", "7️⃣"],
  spinDurationMs: 1200,
  animationTickMs: 90,
};

window.GAME_CONFIG = GAME_CONFIG;
